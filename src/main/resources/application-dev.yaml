spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: password
    url: jdbc:h2:./challengelab;AUTO_SERVER=TRUE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    web: debug